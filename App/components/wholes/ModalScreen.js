import React, { useState } from "react";
import { View, Button } from "react-native";

const ModalScreen = () => {
  return (
    <View>
      <Button title="Income"></Button>
      <Button title="Expense"></Button>
    </View>
  );
};

export default ModalScreen;
